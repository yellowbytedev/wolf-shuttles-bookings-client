# Marketing context router

1. Read repository `AGENTS.md` and `CURRENT-STATE.md`.
2. For public form/UX work, read the approved task and current implementation plus matching fixtures.
3. For payload, signature, redirect, or receiver compatibility, invoke `$ws-handoff-authority-review` and verify both repositories.
4. For WordPress/browser work, use `$ws-localwp-https-qa` and `TEST-COMMANDS.md`.
5. For commit/readiness work, use `$ws-safe-git-promotion`; this does not authorize push or merge.

Current state and verified code override stale progress documents. Historical AI handoffs, roadmaps, and Phase 2 prompts are not standing authorization.
